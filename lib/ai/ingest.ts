/**
 * AI Ingest — text thô → JSON dữ liệu cuối. STUB Phase 1.
 * Triển khai thật (gpt-4o, structured output, chunk, retry) ở Phase 2.
 */
import type { IngestResult } from "./index";

export async function runIngest(text: string): Promise<IngestResult> {
  void text;
  throw new Error("AI Ingest chưa triển khai — Phase 2");
}
